import React from 'react';

const SearchBar = () => {
    return (
        // bg-red-500 is temporary for demonstration purpose only
        <div className="bg-red-500 h-[70px]">
            <h1>Search bar</h1>
        </div>
    );
};

export default SearchBar;